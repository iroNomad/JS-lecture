좋아요! 지금 시점 학생들 수준(조건문·반복문·함수 선언까지, 배열/객체/화살표함수/함수표현식/고차함수는 아직)에 딱 맞춰서, **절차적 코드 → 함수 리팩토링** 문제 5개를 난이도 순으로 준비했어요. 각 문제마다 원본 절차적 코드 전체와 리팩토링 힌트를 같이 드릴게요.

---

## 🟢 문제 1 (난이도 ⭐) — 성인 여부 판별

**절차적 코드**

```javascript
let age = 22;

if (age >= 19) {
  console.log("성인입니다.");
} else {
  console.log("미성년자입니다.");
}
```

**미션**
위 코드를 함수로 리팩토링해보세요. 나이가 바뀌어도 코드를 수정하지 않고, 함수만 다시 호출하면 결과를 확인할 수 있어야 합니다.

**힌트**
- `age`라는 변수를 그대로 함수 밖에 둘 필요가 있을까요? 함수의 **매개변수**로 넘겨보세요.
    - 함수 안에서 바로 `console.log`를 찍기보다, 판별 결과를 문자열로 **return** 하고 호출하는 쪽에서 출력하는 방식도 연습해보세요. (둘 중 편한 방식으로 먼저 해보고, 시간 되면 둘 다 시도해봐도 좋아요)
- 함수 이름은 어떤 동작을 하는지 드러나게 지어보세요 (예: `checkAdult` 같은 식).

---

## 🟢 문제 2 (난이도 ⭐⭐) — 범위 합 구하기

**절차적 코드**

```javascript
let start = 1;
let end = 10;
let sum = 0;

for (let i = start; i <= end; i++) {
  sum += i;
}

console.log(sum);
```

**미션**
시작 값과 끝 값이 항상 1과 10으로 고정되어 있는 게 아쉽네요. 어떤 범위가 들어와도 합을 구할 수 있는 함수로 만들어보세요.

**힌트**
- 매개변수가 몇 개 필요할까요? `start`, `end` 둘 다 함수 바깥에서 정할 수 있어야 합니다.
- `sum`처럼 계산 중간에 쓰이는 변수는 함수 **안에서 선언**해야 호출할 때마다 깨끗하게 초기화됩니다. (함수 바깥에 두면 어떤 문제가 생길지 한번 생각해보세요!)
- 이 함수는 결과를 화면에 그냥 찍고 끝낼까요, 아니면 값을 돌려줘서 다른 곳에서도 쓸 수 있게 할까요? `return`을 써보는 걸 추천해요.

---

## 🟡 문제 3 (난이도 ⭐⭐⭐) — 조건부 구구단 출력

**절차적 코드**

```javascript
let dan = 3;

for (let i = 1; i <= 9; i++) {
  let result = dan * i;

  if (result % 2 === 0) {
    console.log(dan + " x " + i + " = " + result + " (짝수)");
  } else {
    console.log(dan + " x " + i + " = " + result + " (홀수)");
  }
}
```

**미션**
지금은 3단만 출력할 수 있어요. 원하는 단을 넣으면 그 단의 구구단을 짝/홀 표시와 함께 출력해주는 함수로 만들어보세요.

**힌트**
- 반복문과 조건문이 같이 섞여있어도 당황하지 마세요. 함수화할 때는 **로직 자체는 그대로 두고, 바깥에서 결정되던 값만 매개변수로 빼주면** 됩니다.
- 이 문제는 함수가 어떤 값을 return하기보다는, 함수 자체가 "출력하는 일"을 담당하는 게 더 자연스러울 수 있어요. 문제 1, 2와 비교했을 때 왜 그런지 생각해보면 좋아요.
- 실무 감각 팁: 이렇게 "특정 값 하나만 다르고 로직은 반복되는 코드"는 실제 서비스에서도 정말 자주 나와요. 예를 들어 쇼핑몰에서 "카테고리별 상품 목록 출력" 로직이 카테고리마다 코드가 따로 있으면 유지보수가 끔찍하겠죠? 그래서 함수로 묶어두는 습관이 중요합니다.

---

## 🟡 문제 4 (난이도 ⭐⭐⭐⭐) — 뒤섞인 로직 분리하기

**절차적 코드**

```javascript
let korean = 85;
let english = 90;
let math = 78;

let total = korean + english + math;
let average = total / 3;

let grade;
if (average >= 90) {
  grade = "A";
} else if (average >= 80) {
  grade = "B";
} else if (average >= 70) {
  grade = "C";
} else {
  grade = "F";
}

console.log("평균 점수: " + average);
console.log("학점: " + grade);
```

**미션**
이 코드는 사실 "평균 계산"과 "학점 판정"이라는 **서로 다른 두 가지 일**이 한 덩어리로 뭉쳐 있어요. 이걸 **함수 2개**(또는 원한다면 출력까지 포함해서 3개)로 분리해보세요.

**힌트**
- 함수 하나가 여러 일을 동시에 하고 있다면, "이 함수의 역할을 한 문장으로 설명할 수 있는가?"를 기준으로 쪼개보세요. (이걸 프로그래밍 용어로 **단일 책임 원칙**이라고 불러요 — 함수 하나는 딱 한 가지 책임만 지는 게 좋다는 뜻이에요.)
- 평균을 구하는 함수는 국어/영어/수학 점수를 매개변수로 받아서 평균값을 **return**하면 될 것 같아요.
- 학점을 판정하는 함수는 무엇을 매개변수로 받아야 할까요? 점수 3개를 다시 받아야 할까요, 아니면 평균을 구하는 함수의 **결과값을 받아서** 처리하면 될까요? 함수끼리 결과를 주고받는 흐름을 한번 그려보세요.

---

## 🔴 문제 5 (난이도 ⭐⭐⭐⭐⭐) — 소수 개수 세기 (중첩 반복문 + 함수 조합)

**절차적 코드**

```javascript
let min = 2;
let max = 20;
let primeCount = 0;

for (let num = min; num <= max; num++) {
  let isPrime = true;

  if (num < 2) {
    isPrime = false;
  } else {
    for (let divisor = 2; divisor < num; divisor++) {
      if (num % divisor === 0) {
        isPrime = false;
      }
    }
  }

  if (isPrime) {
    primeCount++;
    console.log(num + "은(는) 소수입니다.");
  }
}

console.log("소수 개수: " + primeCount);
```

**미션**
이번엔 한 단계 더 나아가서, **함수 안에서 다른 함수를 호출하는 구조**를 연습해볼게요.
1. 어떤 숫자 하나가 소수인지 아닌지 **판별만** 하는 함수를 하나 만들어보세요.
2. 어떤 범위(min~max) 안의 소수 개수를 세는 함수를 또 하나 만들고, 그 안에서 1번 함수를 반복 호출하도록 만들어보세요.

**힌트**
- "숫자 하나가 소수인지 판별"과 "범위 안의 소수를 세는 것"은 서로 다른 역할이에요. 문제 4에서 연습한 감각을 여기서도 그대로 적용해보세요.
- 소수 판별 함수는 매개변수를 몇 개 받아야 할까요? (숫자 하나면 충분하겠죠?) 그리고 결과는 참/거짓(`true`/`false`)으로 return하는 게 자연스러워요.
- 범위를 세는 함수 안에서 반복문을 돌리다가, 소수 판별 함수를 호출한 **결과값을 조건문에 바로 활용**해보세요. 예를 들면 `if (판별함수(num)) { ... }` 같은 형태가 가능할지 고민해보세요.
- 원래 코드에 있던 이중 for문(중첩 반복문) 중 안쪽 for문은 소수 판별 함수 안으로, 바깥쪽 for문은 개수를 세는 함수 안으로 들어가면 자연스러운 그림이 나올 거예요.

---

    이렇게 5문제 준비했어요. 1~2번은 "절차적 코드를 함수 껍데기로 감싸는 것" 자체에 익숙해지는 단계고, 3번부터는 "왜 굳이 함수로 나눠야 하는가"를 체감하는 단계, 4~5번은 **함수와 함수가 서로 협력하는 구조**를 처음 맛보게 하는 단계로 설계했어요. 필요하시면 각 문제의 예시 답안도 만들어드릴게요!